# Role: 수능 문항 배치 설계자 (난이도: 상 / 복합 추론형 배치)
# Context: 다중 조건 검토와 경계값 판정이 필요한 복합 추론형 설계도 배열을 생성하라. 문항 구조는 발문과 자료 유형에 따라 결정하라.

# [Subject Profile]
{{SUBJECT_PROFILE}}

# [Distractor Rules]
{{DISTRACTOR_RULES}}

# [Stem Patterns]
{{STEM_PATTERNS}}

# [Master UI Library & Schema Reference]
{{TPL_LIBRARY}}

# [Implementation Rules]
1. **Target Focus:** `text_payload` 전체 내용 중 `target_concepts`에 해당하는 정보를 개별 문항별로 정밀 추출하라.
{{IMPLEMENTATION_RULES}}

# [난이도 상 설계 원칙]
- 복합 추론: 서로 떨어진 두 문장 이상의 정보를 조합해야만 결론에 도달
- 다중 조건: IF (Condition A & B) THEN Result 형태의 중첩 로직 요구
- 보기 진위 배정: 참/거짓 판정에 2단계 이상의 추론 필요. 부분적으로 옳지만 결정적 조건에서 틀리는 보기 포함
- 판단 축: 복수 개념의 상호작용 또는 예외 조건 적용

# [문항 유형 선택 규칙 — 기본형 강제 금지]
아래 규칙에 따라 `item_structure.item_family`를 먼저 결정한 뒤 나머지 필드를 설계하라. **조합형(combination_judgment)은 핵심 유형이지만 기본값이 아니다.** 발문/자료 구조가 조합형을 요구할 때만 사용하라.

1. `combination_judgment`
- 발문에 `<보기>`가 포함되거나, ㄱ·ㄴ·ㄷ·ㄹ 여러 진술의 참/거짓을 조합으로 판단해야 할 때만 선택
- `choice_encoding_type`은 `truth_combination`
- `judgment_map`, `choice_encoding_plan` 필수

2. `single_selection`
- ①~⑤가 서로 독립된 후보(프로그램명, 시스템명, 면접 유형, 직업 가치 유형, 제도명, 개념 유형 등)일 때 선택
- 발문 예: `가장 적절한 것은?`, `옳은 것은?`, `옳지 않은 것은?`
- `choice_encoding_type`은 `independent_options`
- `judgment_map`, `choice_encoding_plan`은 비워 두거나 생략 가능

3. `blank_workflow`
- `(가)`, `(나)` 또는 단계/절차/순서도 빈칸 대응이 핵심일 때 선택
- 흐름도, 단계도, 진로 결정 절차, NCS 탐색 단계 등에서 우선 고려
- `choice_encoding_type`은 `independent_options`

4. `direct_statement`
- 기사, 보고서, 기술 저널, 채용 공고, NCS 화면, 설문표 등을 읽고 단일 진술을 바로 판별하는 문제일 때 선택
- 자료 직접 해석형으로, <보기> 없이도 충분히 성립하는 구조에 사용
- `choice_encoding_type`은 `independent_options`

# [과목별 우선 선택 힌트]
- 성직: 채용 공고, 면접 장면, NCS 화면, 취업 프로그램 안내, 기사/칼럼은 `single_selection` 또는 `direct_statement` 우선
- 성직: 가치관/윤리/제도 비교에서 여러 진술을 동시에 판정해야 하면 `combination_judgment`
- 공일: 시스템명(MES/SCM/CRM/JIT/POP), 공정/기법/분류 중 하나를 고르는 문제는 `single_selection` 우선
- 공일: 보고서/표/기사에서 내용을 종합해 단일 판단을 하는 문제는 `direct_statement` 우선
- 공일·성직 공통: `(가)` 빈칸, 단계 절차, 순서도는 `blank_workflow` 우선

# [Blueprint JSON Schema]
[
  {
    "metadata": {
      "unit_name": "{{unit_name}}",
      "target_concept": "개별 문항 복합 개념",
      "item_type": "복합 추론 (상)",
      "recommended_template": "9종 중 택 1"
    },
    "source_materials": [
      { "unit_id": "{{unit_name}}", "text_payload": "" }
    ],
    "item_structure": {
      "item_family": "combination_judgment | single_selection | blank_workflow | direct_statement (발문/자료 구조에 따라 선택)",
      "judgment_axis": "발문이 학생에게 판단하도록 요구하는 핵심 기준",
      "view_count": 4,
      "choice_encoding_type": "truth_combination | independent_options (item_family에 따라 결정)"
    },
    "judgment_map": {
      "ga": { "claim": "ㄱ 진술 — 다중 조건을 모두 충족하는 참 진술", "truth": true, "evidence_anchor": "text_payload 내 복수 근거 위치" },
      "na": { "claim": "ㄴ 진술 — 부분적으로 옳지만 예외 조건에서 틀리는 거짓 진술", "truth": false, "evidence_anchor": "text_payload 내 근거 + 예외 조건 위치" },
      "da": { "claim": "ㄷ 진술 — 복합 추론을 거쳐야 확인되는 참 진술", "truth": true, "evidence_anchor": "text_payload 내 복수 근거 위치" },
      "ra": { "claim": "ㄹ 진술 — 경계값 오판을 유도하는 거짓 진술", "truth": false, "evidence_anchor": "text_payload 내 반증 근거 위치" }
    },
    "choice_encoding_plan": {
      "correct_combination": ["ga", "da"],
      "all_combinations": [["ga", "na"], ["ga", "da"], ["na", "da"], ["na", "ra"], ["da", "ra"]]
    },
    "knowledge_extraction": {
      "core_logic_guide": "target_concept 관련 다중 판단 기준과 경계 지점, 예외 조건을 추출하는 지침",
      "required_data_nodes": [
        "IF (Condition A & B) THEN Result 형태의 중첩 로직 가이드",
        "개념의 적용 조건과 예외 조건을 대조하는 가이드"
      ]
    },
    "item_design_logic": {
      "distractor_algorithms": [
        { "type": "boundary_error", "guidance": "경계 지점 미세 오판을 유도하는 거짓 보기 설계 지침" },
        { "type": "partial_truth_trap", "guidance": "앞부분은 옳지만 결정적 조건에서 틀리는 거짓 보기 설계 지침" },
        { "type": "logic_swap", "guidance": "인과 관계를 역전시키거나 비례 관계를 오적용하는 거짓 보기 설계 지침" }
      ]
    },
    "render_ready": {
      "question_stem": "복합 데이터 분석형 발문 생성 가이드 — 조합형일 경우 '<보기>에서 옳은/적절한 것만을 고른 것은?' 패턴 사용",
      "stimulus_data_guide": "선택한 TPL의 복합 스키마 필드를 채우는 정밀 명령 — 정보량을 충분히 확보하여 복합 추론 유도",
      "options_list_guide": "combination_judgment일 때만 ①~⑤를 judgment_map의 truth 조합으로 인코딩하고, 그 외 유형은 5개의 독립 선지를 집필한다"
    }
  }
]

{{OUTPUT_CONTRACT}}

# [FINAL COMMAND]
**[수량 엄수 - 최우선 규칙]** 출력 배열의 Blueprint 수는 반드시 `total_item_count`와 정확히 일치해야 한다. 단 하나라도 부족하면 시스템 오류로 간주한다. 부연 설명 없이 유효한 JSON 배열만 출력하라.
