# Progress

- The old final-generation contract intentionally excluded source display text; it is now replaced by `referenceSource` plus `fidelityContract`.
- `ReferenceFidelitySpec` currently derives deterministic identity, concept, response topology, density, and protected source segments from a selected reference.
- Source labels now resolve to catalog IDs and planner payloads reject a catalog-valid target that is not the selected source target.
- The final output path now rejects verbatim source segments and stimulus data below half the source density, while allowing fixed combination-choice topology.
- Lineage now retains a non-sensitive contract version/source hash/response/density receipt; the full semantic verifier and three-fixture live QA remain pending.
- Focused tests and typecheck pass; global lint remains blocked by existing formatting errors outside this work.
- Final verification F2 is marked blocked in the plan until the unrelated repository-wide lint backlog is resolved.
- Final structured output now requires machine-readable `sourceEvidence` with the selected source hash and target concept IDs. The regenerator rejects missing or mismatched evidence before renderability, copy, and density checks; legacy envelope repair preserves the equivalent snake_case field.
- Focused reference suite (7 suites, 72 tests) and `npm run typecheck` pass after propagating the evidence contract through schema and integration fixtures.
- Added `reference-variant-semantic-verifier.ts`, a fail-closed parser for the verifier's strict `{ accepted, reasonCode }` JSON verdict.
- Semantic verification is now invoked before accepted results are returned. Accepted results carry a non-sensitive receipt (deterministic/copy pass markers, verifier model/verdict/reason code, retry count), and `ReferenceFrameGenerationService` merges it into lineage fidelity for Question persistence.
- Next implementation target: add `ExamsService` transaction-level assertions that the merged receipt is saved as `Question.generationLineage.fidelity.receipt`, and that a reference-generation shortfall reaches no transaction writes.
- Receipt integration assertion confirms the artifact contains no `referenceSource` or candidate payload; focused integration test and typecheck pass.
- `ReferenceGenerationStaging` now has a regression proving a missing slot after semantic rejection invokes only the failed-run audit and never writes complete questions; focused test and typecheck pass.
- Focused task-7 command passes: `exam-regenerator.reference-variant.spec.ts`, `reference-frame-generation.integration.spec.ts`, and `reference-generation-persistence.spec.ts` (39 tests), plus typecheck.
- Live QA is blocked: `npm run test:reference-live` requires `REFERENCE_LIVE_QA=1`; it performed cleanup only and did not claim a live run.
- Existing `exams.service.spec.ts` and `reference-generation-persistence.spec.ts` pass alongside typecheck; transaction-specific receipt assertions remain the next implementation gap.
- Task 1 acceptance evidence: `reference-archetype.golden.spec.ts` distinguishes combination table (`sungjik:15:1`), condition/exception document (`sungjik:15:9`), and single-selection case (`kongil:15:8`) fixtures; `exam-regenerator.reference-variant.spec.ts` proves a unique source token appears only in `referenceSource` and not unrelated prompt fields. Focused Jest: 3 suites/44 tests passed; `npm run typecheck` passed.
- Task 2 acceptance evidence: `reference-fidelity-spec.spec.ts` now validates the combination table (`sungjik:15:1`), condition/exception document (`sungjik:15:9`), and single-selection case (`kongil:15:8`) contracts for exact source identity, canonical target ID, response topology, density, relation ordering, and option mappings. It rejects stale contract version/source hash, unknown targets, duplicate unit IDs, cyclic/unknown relations, and incomplete option maps. `reference-frame-generation.service.spec.ts` proves stale cache version and embedded source hash entries rerun the frame planner rather than reuse the cached frame. Focused Jest: 4 suites/52 tests passed; `npm run typecheck` and `git diff --check` passed. TypeScript LSP diagnostics could not run because the server is not installed.
- Task 3 acceptance evidence: `reference-frame-generation.service.spec.ts` proves `sungjik:15:1`, `sungjik:15:9`, and `kongil:15:8` each preserve the resolved source catalog ID as `payload.targetConceptIds[0]`. It rejects unknown, out-of-range, and caller-excluded source targets before `regenerateReferenceBatch`; the caller-exclusion path exposes `SOURCE_TARGET_EXCLUDED`. Selector coverage proves a supporting label cannot satisfy canonical-target eligibility, and planner coverage rejects catalog-valid supporting concepts absent from the source contract. Focused Jest: 4 suites/50 tests passed; `npm run typecheck` and `git diff --check` passed. TypeScript LSP diagnostics could not run because the server is not installed.
- Task 4 acceptance evidence: `exam-regenerator.reference-variant.spec.ts` verifies the combination-table (`sungjik:15:1`), condition/exception document (`sungjik:15:9`), and single-selection (`kongil:15:8`) prompts each contain source prose only in `referenceSource`, while retaining relation/density contract fields, transformation policy, and the selected TPL. The prompt now omits source-only protected segments from `fidelityContract`. Singleton schema evidence remains required; batch parser tests reject missing evidence, wrong template, changed topology, and item-count mismatch without retaining an earlier valid result. Plain-text prohibition and singleton-only bounded correction remain covered. Focused Jest: 4 suites/60 tests passed; `npm run typecheck` and `git diff --check` passed. TypeScript LSP diagnostics could not run because the server is not installed.
- Task 5 acceptance evidence: deterministic fidelity trace tests reject inverted verdicts and missing distractor roles; semantic verifier tests reject omitted exceptions, inverted verdicts, absent roles, malformed responses, negative verdicts, and simulated timeout after exactly one correction retry with no retained candidate. Copy policy now rejects unapproved normalized 24-character overlaps plus substantive copied source choices/sentences while allowing 23-character overlap; density has lower and upper bounds. Semantic reason codes are strict identifiers, preventing raw verifier prose from reaching correction input. Focused Jest: 5 suites/72 tests passed; `npm run typecheck` and `git diff --check` passed. TypeScript LSP diagnostics could not run because the server is not installed.
- Task 6 acceptance evidence: `Question.generationLineage` persists the existing source ID/hash, contract version/hash, deterministic/copy-policy pass receipt, semantic verifier model/verdict/reason code, and retry count through the live reference exam transaction without source display text or raw completion data. A zero-result fidelity run now exposes `REFERENCE_FIDELITY_REJECTED`; mixed semantic shortfall exposes `REFERENCE_GENERATION_SHORTFALL` before opening the transaction, leaving Question and ExamItem writes empty. Job failures retain a structured `errorCode`, a fixed safe message, and code-only log detail, so raw exception payloads cannot reach the job surface. Focused Jest: 5 suites/75 tests passed; `npm run typecheck` and `git diff --check` passed. TypeScript LSP diagnostics could not run because the server is not installed.
- Task 7 implementation evidence (blocked): integration coverage now has separately asserted analyzer, final-generator, and semantic-verifier mocks, and selected catalog source identity/contract hash propagates into generated lineage. The live harness writes redacted per-fixture JSON/Markdown receipts containing only source/output hashes, deterministic/copy results, verifier model/verdict/reason, retry count, and pass status; unconfigured `npm run test:reference-live` deliberately prints `{"status":"skipped","reason":"REFERENCE_LIVE_QA_NOT_ENABLED"}` without attempting a mocked run. Focused Jest: 5 suites/71 tests passed; `npm run typecheck` passed. Task 7 remains unchecked because full Jest fails in pre-existing `reference-generation-metrics.spec.ts` (`INCOMPLETE_OPTION_MAPPING:structure.optionMappings`) and full lint fails on existing repository-wide Prettier violations. Live credentials/configuration remain unavailable, so no real-provider artifact was produced. TypeScript LSP diagnostics could not run because the server is not installed.
- F4 scope-fidelity and persistence audit: APPROVE. `ExamsService.createReferenceFrameExam` validates generated count before opening `examRepo.manager.transaction`, saves each `Question` with the merged `generationLineage.fidelity.receipt`, then saves the exam and items in the same transaction. Persistence coverage was strengthened in `exams.persistence.spec.ts`: the accepted path asserts the receipt reaches `questionRepo.save` without source prose/raw completion; the background shortfall asserts no transaction, repository lookup, manager save, question save, or exam-item creation; the synchronous shortfall asserts the same no-write boundary. Focused Jest (`exams.service.spec.ts`, `exams.persistence.spec.ts`, `reference-generation-persistence.spec.ts`, `reference-frame-generation.integration.spec.ts`): 4 suites/11 tests passed. `npm run typecheck -w backend` and `git diff --check` passed. No Postgres-backed Jest harness exists in this repository, so the evidence is at the TypeORM repository/transaction seam rather than a live database row query. TypeScript LSP diagnostics remain unavailable because the server is not installed.
- F1 plan-compliance audit (2026-07-24): **REJECT**. The plan's Out section excludes the `sourceType: 'ai'` pipeline/general exam API, frontend option/display changes, existing-question backfill/regeneration, and catalog-import-format changes; the execution strategy also explicitly says no DB migration. The current worktree is not scope-isolated: `git diff --cached --name-only` reports 158 staged paths, `git diff --name-only` reports 54 unstaged tracked paths, and `git ls-files --others --exclude-standard` reports 425 untracked paths.
- Scope evidence: the current status includes 163 `frontend/` paths; `frontend/app/(main)/study/[subject]/[chapter]/q3/page.tsx:62-68` changes the study cache to select `sourceType === 'reference'`, and `frontend/components/exam/CreateExamModal/index.tsx:75` changes request serialization based on the AI/reference selector. These are frontend behavior changes in an explicitly excluded area.
- Scope evidence: the untracked `backend/src/migrations/` directory contains seven TypeORM migration files, including `AddExamSourceType`, `AddQuestionGenerationLineage`, `CreateReferenceQuestions`, `CreateGenerationRunTables`, `CreateReferenceFrameCache`, and `AddReferenceArchetypeCacheVersion`; this directly violates the no-migration constraint. The changed backend also contains AI-path branches at `backend/src/exams/exams.service.ts:110,145,600,636`, plus the new AI-only QA scripts `backend/scripts/ai-unit-qa.ts:60-70` and `backend/scripts/normal-ai-live-qa.ts:280-318`.
- Scope evidence: no changed path is named `backfill`, but the added `backend/src/textbook/import-reference-catalog.ts:30-35`, `backend/src/textbook/reference-catalog-import.service.ts:112-157`, and `backend/src/exams/generation-data-reset.service.ts:35-41` introduce corpus import/reset operations outside the reference-faithful generation plan and must not be bundled as F1-approved work.
- Verification evidence: `npm test -- --runInBand exam-regenerator.reference-variant.spec.ts reference-frame-generation.integration.spec.ts reference-generation-persistence.spec.ts` passed 3 suites/55 tests; `npm run typecheck` passed; `git diff --check` and `git diff --cached --check` both passed with no output. These green checks validate the focused reference surface but do not override the failed scope gate. No application code was modified during this audit.
- Follow-up verification (2026-07-24): updated the reference-only legacy metrics fixture so its archetype, five option mappings, and five answer-plan options agree with the enforced source-fidelity contract. `reference-generation-metrics.spec.ts` now passes; full Jest passes 48 suites/299 tests; `npm run typecheck` and `git diff --check` pass. The reference fidelity module formatting violations were fixed, but repository-wide `npm run lint` still fails on unrelated existing files. `npm run test:reference-live` remains an explicit `REFERENCE_LIVE_QA_NOT_ENABLED` skip because no authorized live backend/database/credential configuration is present.
